# Keep the build context small + reproducible — the image installs deps fresh
# from the lockfile and builds inside the container, so local artifacts must
# not leak in.
node_modules
.next
out
.git
.gitignore
.env*
Dockerfile
.dockerignore
npm-debug.log*
.DS_Store
.vscode
.idea
README.md
